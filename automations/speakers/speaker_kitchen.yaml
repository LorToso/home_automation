- id: 'kitchen_speaker_reset'
  alias: Kitchen Speaker Reset
  description: ''
  trigger:
  - platform: state
    entity_id: media_player.kitchen_speaker
    to: 'paused'
    for: 3600
  - platform: state
    entity_id: media_player.kitchen_speaker
    to: 'idle'
    for: 3600
  condition: []
  action:
  - service: media_player.volume_set
    target:
      entity_id: media_player.kitchen_speaker
    data:
      volume_level: 0.2
  - service: sonos.unjoin
    data: {}
    target:
      entity_id: media_player.kitchen_speaker

- id: 'kitchen_speaker_join_group'
  alias: Kitchen Speaker Join Group
  description: ''
  trigger:
  - platform: state
    entity_id: input_boolean.kitchen_presence
    from: 'off'
    to: 'on'
    for: '0'
  - platform: state
    entity_id: input_boolean.bar_presence
    from: 'off'
    to: 'on'
    for: '0'
  condition:
  - condition: state
    entity_id: input_boolean.kitchen_motion
    state: 'on'
  - condition: state
    entity_id: input_boolean.music_following
    state: 'on'
  action:
  - choose:
    - conditions:
      - condition: state
        entity_id: binary_sensor.bath_room_speaker_group_leader
        state: 'on'
      sequence:
      - service: sonos.join
        data:
          master: media_player.bath_room_speaker
          entity_id: media_player.kitchen_speaker
    - conditions:
      - condition: state
        entity_id: binary_sensor.living_room_speaker_group_leader
        state: 'on'
      sequence:
      - service: sonos.join
        data:
          master: media_player.living_room_speaker
          entity_id: media_player.kitchen_speaker
    - conditions:
      - condition: state
        entity_id: binary_sensor.office_speaker_group_leader
        state: 'on'
      sequence:
      - service: sonos.join
        data:
          master: media_player.office_speaker
          entity_id: media_player.kitchen_speaker
    default: []
